# Windows / CPython 3.14 local compatibility baseline.
# SciPy 1.18.1 stalled importing stats._qmvnt_cy on the tested machine.
# This is a scoped workaround, not a claim that all 1.18 installations fail.
scipy==1.16.3; sys_platform == "win32" and python_version == "3.14"
